// /** @type {import('next').NextConfig} */
// const nextConfig = {};

// module.exports = nextConfig;

module.exports = {
  eslint: {
    // dirs: ["pages", "utils"],
     ignoreDuringBuilds: true,
  },
};
